function showdiv(id){
 
if(showdiv_display = document.getElementById(id).style.display=='none'){//如果show是隐藏的
 
document.getElementById(id).style.display='block';//show的display属性设置为block（显示）
 
}else{//如果show是显示的
 
document.getElementById(id).style.display='none';//show的display属性设置为none（隐藏）
 
}
 
}
